[pytest]
pythonpath = .
testpaths = tests
# Tests that load ResNet-50. Deselected by default so the suite never downloads
# 100 MB of weights; run them with `pytest -m slow`.
addopts = -m "not slow"
markers =
    slow: needs the real CNN weights
filterwarnings =
    ignore::DeprecationWarning
